# macos-setup
A repo for getting started fast with a new mac computer

## Step 1
Download the `init.sh` script and run it. This will set up git and the required ssh keys.
Once the ssh key is generated, add it to this repo's Deploy Keys and clone the repo.

## Step 2
Run `brew-apps.sh`. This will install a bunch of common software.
